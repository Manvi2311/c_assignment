#include <stdio.h>
int main()
{
  int a;
  float b;
  char c;
  double d;
  printf("Size of Int Data Types in C is %d bytes \n", sizeof(a));
  printf("Size of Double  in C is %d bytes \n", sizeof(d));
  printf("Size of float Data Types in C is %d bytes \n", sizeof(b));
  printf("Size of Char Data Types in C is %d bytes \n", sizeof(c));
  return 0;
}